# dotfiles
baseline dotfiles

define variables locally or within the .bashrc for aws instance ids and ec2 usernames for lines 26-30

using the aliases to connect will prevent you from having to manually pull the IP from the console or aws cli each time you restart the instance as opposed to paying for elastic-ips. configuring local aws cli credentials is required.